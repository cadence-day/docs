{
  "name": "cadence-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "doppler run --command 'docusaurus start'",
    "build": "doppler run --command 'docusaurus build'",
    "swizzle": "docusaurus swizzle",
    "deploy": "doppler run --command 'docusaurus deploy'",
    "clear": "docusaurus clear",
    "serve": "doppler run --command 'docusaurus serve'",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc",
    "lint": "echo 'Linting complete - no linter configured'",
    "test": "npm run typecheck && npm run build",
    "preview": "npm run build && npm run serve"
  },
  "dependencies": {
    "@docusaurus/core": "3.9.1",
    "@docusaurus/preset-classic": "3.9.1",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "phosphor-react": "^1.4.1",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.9.1",
    "@docusaurus/tsconfig": "3.9.1",
    "@docusaurus/types": "3.9.1",
    "typescript": "~5.6.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
