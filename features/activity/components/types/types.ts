// This file used to contain component-level activity types.
// Types are now centralized in `features/activity/types.ts`.
// Re-export everything so existing imports keep working.

export * from "../../types";
